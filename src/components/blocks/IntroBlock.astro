---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<div class={`case-study-copy mx-auto max-w-3xl px-6 md:px-0 ${className}`}>
  <div class="case-study-lede text-2xl leading-relaxed md:text-3xl">
    <slot />
  </div>
</div>
